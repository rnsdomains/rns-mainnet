{
  "name": "rns",
  "version": "0.0.1",
  "private": true,
  "description": "Implementations for registrars and local resolvers for the RSK Name Service, adapted from Ethereum Name Service",
  "scripts": {
    "test": "mocha"
  },
  "dependencies": {
    "bignumber.js": "^7.2.1",
    "eth-ens-namehash": "^2.0.8"
  },
  "devDependencies": {
    "mocha": "^3.2.0"
  }
}
